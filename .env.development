# 开发环境配置

# 数据模式
# mock - 使用简单模拟数据（少量节点）
# generator - 使用数据生成器（大量节点，推荐）
# v2 - 使用 V2 实验数据（1368个节点，真实数据结构）
# api - 使用导入的 Excel 数据（从 localStorage 读取）
VITE_DATA_MODE=api

# API 配置（仅在 api 模式下使用）
# 后端 API 地址（当前使用 localStorage，不需要配置）
VITE_API_URL=http://localhost:3000/api/equity
